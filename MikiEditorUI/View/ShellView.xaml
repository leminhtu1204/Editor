<Window x:Class="MikiEditorUI.View.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="ShellView" Height="768" Width="1024">

    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <MenuItem Header="_Open"/>
                <MenuItem Header="_Close"/>
                <MenuItem x:Name="MenuSave" Header="_Save"/>
            </MenuItem>
        </Menu>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar HorizontalAlignment="Right" BorderThickness="0">
                <Button Command="New" Content="New" Height="32" Width="48" Margin="0,0,20,0" />
                <Separator></Separator>
                <Button x:Name="BtnSave" Command="Save" Content="Open" Height="32" Width="48" Margin="0,0,20,0"/>
            </ToolBar>
        </ToolBarTray>
        <Grid DockPanel.Dock="Bottom">
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="200"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition MinWidth="500"></ColumnDefinition>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition MinWidth="300"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <Grid Name="LeftGrid" Grid.Column="0" Margin="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="32"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" HorizontalAlignment="Center" DockPanel.Dock="Top" MinHeight="24">Page Navigator</Label>
                <ListView Grid.Row="1" MinHeight="650" DockPanel.Dock="Top"> list view</ListView>
                <Button Grid.Row="2" DockPanel.Dock="Top" MinHeight="24" Name="BtnAddPage">Add Page</Button>
            </Grid>
            
            <DockPanel Name="MiddleGrid" Grid.Column="2" Margin="3">
                <Label>Image</Label>
            </DockPanel>

            <Grid Name="RightGrid"  Grid.Column="4" Margin="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="32"></RowDefinition>
                    <RowDefinition Height="*"></RowDefinition>
                    <RowDefinition Height="32"></RowDefinition>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" HorizontalAlignment="Center" DockPanel.Dock="Top" MinHeight="24">Chapter</Label>
                <ListView Grid.Row="1" MinHeight="650" DockPanel.Dock="Top">list view</ListView>
                <Button Grid.Row="2" DockPanel.Dock="Top" MinHeight="24" >Add New Chapter</Button>
            </Grid>
            <GridSplitter Grid.Row="0" Grid.Column="1" Grid.RowSpan="2"
        Width="2" VerticalAlignment="Stretch" HorizontalAlignment="Center"
        ShowsPreview="True"></GridSplitter>
            <GridSplitter Grid.Row="0" Grid.Column="3" Grid.RowSpan="2"
        Width="2" VerticalAlignment="Stretch" HorizontalAlignment="Center"
        ShowsPreview="True"></GridSplitter>
        </Grid>
    </DockPanel>
</Window>
